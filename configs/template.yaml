data:
  dataset_name: 'starmap_merfish_mouse_brain_2_gene_panels'
  raw_data_folder_path: ''
  tokenizer_type: 'cell_neighborhood' # 'cell_neighborhood', 'cell_graph'
  data_path: '/lustre/scratch126/cellgen/team361/am84/NicheJEPA/datasets/am84/st_data/gold/cell-neighborhood-tokenizer/starmap_merfish_mouse_brain_harmonized_2-gene-panels/16-shards/starmap_merfish_mouse_brain_harmonized_2-gene-panels_4_False_read_depth_shifted_log_3338.dataset'
  random_state: 0
  sample_subset: False
  split: 0.2
  stratify: False
  vocab_size: 1671
  seq_len_cell: 176 # 353
  seq_len_neighborhood: 309 # 619
  n_segments: 2
  sampling_strategy: 'norm_count_rank_sampling' # null, 'random_sampling', 'norm_count_rank_sampling', 'random_sampling_rep', 'norm_count_rank_sampling_rep'
  batch_size: 64 # 64
  num_workers: 6
  pin_memory: true
meta:
  special_tokens: ['cls_cell', 'cls_neighborhood', 'assay', 'species', 'tissue', 'gene_panel', 'batch']
  enc_depth: 3 # 3
  checkpoint_freq: 5
  log_freq: 20
  load_checkpoint: false
  load_timestamp: ''
  pred_depth: 1 # 1
  pred_emb_dim: 768
  enc_emb_dim: 768
  enc_pred_depth: 31
  top_k: 127
  read_checkpoint: null # 'jepa-ep5.pth.tar'
  use_bfloat16: True
  pos_learnable: False
  seg_learnable: False
mask:
  context_mask_size: 1220 # This doesn't have meaning in segment_masking.
  target_mask_size: 10 # This doesn't have meaning in segment_masking.
  n_contexts: 1
  n_targets: 5 # This is equivalent to n_segment in segment_masking.
  per_segment_mask_ratio: 0.5
  segment_masking: True
optimization:
  epochs: 20
  warmup: 3
  ema:
  - 0.9764319982153814
  - 1.0
  start_lr: 2.0e-05
  lr: 2.0e-05
  final_lr: 2.0e-05
  weight_decay: 0.04
  final_weight_decay: 0.4
  ipe_scale: 1.0
logging:
  write_tag: nichejepa
